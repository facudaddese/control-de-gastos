import Form from "../form/Form";

const MainLayout = () => {
  return (
    <main className="flex items-center justify-center mx-auto p-7">
      <Form />
    </main>
  );
};

export default MainLayout;
